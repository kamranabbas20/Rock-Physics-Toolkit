"""Well-data ingest and unit standardisation.

Note: this package is named ``avo_qi.io`` and never shadows the standard
library ``io`` module, because it is only ever imported by its fully
qualified name.  Import it as ``from avo_qi.io import loader``.
"""
